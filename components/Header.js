import React from "react";
import styles from "./Header.module.css";

const Header = () => (
  <div className={styles.Header}>
    <div className="container p-3 mb-3 display-3 text-center">
      Shopping List
    </div>
  </div>
);

export default Header;
